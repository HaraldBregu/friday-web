# Project Notes

This is an Astro static site.

- Use Astro components, content collections, JSON data files, and custom CSS.
- Keep output static with `output: "static"` in `astro.config.mjs`.
- Do not add framework UI libraries or Tailwind.
- Prefer small, accessible components and data-driven content.
